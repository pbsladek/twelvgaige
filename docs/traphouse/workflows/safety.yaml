kind: workflow
id: safety_simple
name: Safety Gate Demo
version: 1.0.0
shots:
  - id: approval
    kind: safety
    description: "Review before continuing"
  - id: after
    kind: slug
    agent: mock_agent
    depends_on:
      - approval
    prompt: "run after approval"
